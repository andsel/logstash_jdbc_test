From the the unpacked directory run:
- `docker-compose -f docker-compose.yml build` to build the image of MySQL with some tables
- `docker-compose -f docker-compose.yml up` to run the MySQL and PostgreSQL
